# Comando útil: Cmd+Shift+P -> Create R Terminal
# Para ejecutar por línea -> Selecciona la línea o el bloque de código que quieres ejecutar. Cmd+Enter para ejecutar la selección en la terminal de R.
# Para instalar paquetes install.packages("nombre_del_paquete")

# Para hacer celdas:



# %% Celda 1
vector_enteros <- c(1, 2, 3, 4)
print(vector_enteros)
# %% Celda 2
suma <- sum(vector_enteros)
print(suma)


iris
